"""Portfolio arm: resumable generic repository tools and compact diff index."""

from _portfolio_generic import PAGED_GENERIC_AGENT
from _runner import main

if __name__ == "__main__":
    main(PAGED_GENERIC_AGENT)
